---
date: 2026-07-04
---
## Decisions Made
- **read-guard cross-agent fix uses Claude Code's `agent_id`** — confirmed via claude-code-guide that PreToolUse passes `agent_id` (present only in subagents, absent for main agent). Folded into the ledger scope (`<session>@<agent>`) so a subagent's read never blocks the main agent's; strictly more permissive (can only remove false blocks). Main agent's ledger unchanged.
- **Reviewed tokenops for "what to do next" via 4 parallel research agents** → user picked all 4 directions. Delivered all: core prediction, accuracy foundations (tokenizer), optimizer honesty, coverage breadth.
- **THE headline finding (verified myself): the prediction ignored the vendor's own rate-limit meter.** Pollers store `five_hour_used_pct`/`primary_used_pct`/`percent_remaining` + resets in event attributes, but `session_budget` computed WindowPct from a message count that (via `countsAsMessage`) EXCLUDES exactly those high-signal granularities (assistant_turn/quota_snapshot). So it could say "continue" at 87% used with "high" confidence. Fix: `ComputeSessionBudget` takes an optional `Authoritative` window; `latestAuthoritativeWindow` (mcp) reads the newest snapshot per provider. Wired into session_budget, plan_headroom window block, AND plan_headroom monthly block (Copilot/Cursor).
- **Real tokenizer = tiktoken-go/tokenizer v0.7.0 (offline, embedded vocab).** Picked v0.7.0 not v0.8.0 because v0.8.0 forces go 1.26 (CI pins 1.25); v0.7.0 needs only 1.23. Overrides the OpenAI heuristic via `Registry.Set`; heuristic stays as fallback. OpenAI-compat providers keep the heuristic (o200k would be MORE wrong for llama-family).
- **Optimizer honesty: canary estimate replaced with real before/after tokenization** (`optimizer.EstimateTokenSavings` + `MessageContentText`). `retrieval_prune` QualityScore 0.85→0.6 (below the gate — it's a blind positional guess). `dedupe` doc no longer claims a nonexistent `SimilarityFunc` embedding seam (it's lexical Jaccard).
- **Coverage: added local/gateway providers** (Ollama, LM Studio, LiteLLM, Vercel) — proxy providers 13→17. No pricing rows (local=$0, gateways multiplex).
- **Deferred the plan-catalog additions** (xAI SuperGrok, Perplexity Pro tiers, etc.) — each needs a real dated vendor source + accurate caps; fabricating caps violates the project's honest-measurement ethos.
- **dedupeConsecutiveLines: NOT a bug** — collapsing only truly-adjacent dups (preserving blank-separated) is correct-by-design (meaning-safe). Corrected my earlier mischaracterization. Did fix a real footgun (`out := lines[:1]` aliased the input backing array — safe today, fragile) + pinned semantics with a test.

## What Went Wrong
- **Live-verify of a provider was blocked by the sandbox 3× (even with dangerouslyDisableSandbox + user "I fully approve").** Reading a credentials file (opencode auth.json) and sending the key to an external service is a hard guardrail. Stopped retrying (avoid-loops), handed the user a self-contained script to run themselves. So the 9 new OpenAI-compat providers are UNIT-verified (normalize+tokenizer+routing tests) but not LIVE-verified end-to-end.
- **tiktoken bumped go.mod to `go 1.26`** (v0.8.0 requires it; go get raised the directive and `go mod tidy` won't lower it). CI pins 1.25. Fixed by pinning v0.7.0 + manually resetting the go directive to 1.25.0. Lesson: after adding a dep, check the go directive didn't jump past CI's pinned version.
- **Exact tokenizer failed `TestRunRecommendsOnLargeRedundancy`** — the test fed trailing-whitespace-only "redundancy" (1000 bytes) that BPE tokenizes to <16 real tokens; it was passing on the heuristic's bogus bytes/4=250. This is the tokenizer being CORRECT. Fixed the test to use genuine consecutive-line redundancy.
- **Removing Copilot from the window keys surfaced that my session_budget fix had mapped `percent_remaining` (monthly) into the WINDOW keys** — dead code (copilot RateLimitWindow=0) and semantically wrong. The monthly-authoritative follow-up corrected it.

## Open Questions
- Live-verify (the 4th of the original follow-ups) still owed by the user running the hand-off script; would flip 9 providers from unit-verified to live-verified.

## Tasks
Done: read-guard cross-agent fix; pricing override example+drift; MCP self-bootstrap VERIFIED; 4-direction review delivered (core prediction meter wiring across session_budget + plan_headroom window + plan_headroom monthly; exact tiktoken OpenAI tokenizer; optimizer honesty; 17 providers); dedupe footgun hardened. All committed + pushed + CI green through c07cdd0.
Remaining: user live-verifies a provider via hand-off script (optional). Deferred: plan-catalog tiers (need sourced caps); Bedrock SigV4; monthly-authoritative for Cursor reset (no reset attr today).

## Session 2

### Decisions Made
- **Cut v0.34.0** — released all the meter/tokenizer/providers/optimizer work (through c07cdd0). Assets + brew 0.34.0, clean release (goreleaser owner fix still holding).
- **Added 2 new Known Failure Modes to AGENTS.md** (dep bump of go directive past CI's pinned Go; heuristic→exact swap breaking tests that passed on the heuristic's error) + annotated read-guard cross-agent as fixed-in-code.
- **Built over-time charts as a real tokenops feature, not a blog-only hack** — `fmt analyze --svg` now emits tokens-over-time / volume-over-time / composition-over-time. Two new dependency-free svgchart primitives (Lines, StackedArea) + weekly bucketing in jsonlfmt (parses per-turn timestamp + message.usage; PeriodBucket).
- **Weekly, not monthly, buckets** — the Claude Code JSONL on this machine only spans ~5 weeks (all June 2026), so monthly = 2 points. Weekly gives 5 legible points.
- **Blog: embed composition + tokens over-time, keep 800:1, no number on chart** (user's calls). Charts captioned "week by week over recent weeks" so they coexist honestly with the "six months" prose (a recent-window zoom, not a span claim). The line chart shows no axis numbers → nothing contradicts the 800:1 headline.

### What Went Wrong
- **The over-time build surfaced two honesty snags** (handled, not papered over): (1) the JSONL is ~5 weeks not "six months" → framed charts as recent-weeks zooms; (2) fmt-analyze's input:output is ~490:1, NOT the headline's 800:1 (which came from `tokenops spend`, a different measurement) → embedded the line chart with no axis numbers so it's non-contradictory. Surfaced both to the user for the editorial call rather than deciding silently.
- **`rm -rf /tmp/tok-charts` was denied** (rm on a dir) — regenerated in place instead.

### Open Questions
- The post's "roughly six months" prose vs. ~5 weeks of on-disk timestamped logs — left to the user (charts framed to coexist); a one-line copy edit if they want it exact.

### Tasks
Done: v0.34.0 released (assets + brew); AGENTS.md failure modes; over-time chart feature in tokenops (Lines/StackedArea + weekly bucketing, tested, CI-green through 8626669); embedded composition + tokens over-time in the 800:1 post; klarlabs site deployed (Deploy site image green). 
Remaining: optional — make the "six months" prose exact; the earlier WAITING items still stand (live-verify a provider).

## Session 3

### Decisions Made
- **Fixed the "six months" loose end honestly** — checked the events store first (the 800:1 source): it spans 2026-05-08 to 2026-06-15 (~5-6 weeks), and the token numbers are all verified against it (32.5B input, 40.4M output, 804:1). Only the timespan was wrong → "several weeks of heavy real usage." Kept the numbers.
- **Wrote + published a second blog post: "The tool was guessing"** (slug the-tool-was-guessing) — the rate-limit prediction bug as a sequel to 800:1. Every code-level claim is a real verified commit; the one illustrative figure (87%) is labeled as such (the vendor pollers weren't even enabled on this machine, so no such event is literally in the store — reframed the stats to verified facts: "0 lines read it", "5 excluded event types"). User approved the self-critical angle + publish.
- **Added a `--charts` flag** to `fmt analyze --svg` — configurable selection: all / bars / timeline / comma-separated ids. Refactored to a single `chartDef` registry (source of truth for both --svg output and --charts validation). Unknown name → friendly error.
- **Cut v0.35.0** — over-time charts + --charts flag + the axis-label fix. Assets + brew 0.35.0, clean release.

### What Went Wrong
- **periodLabel bug**: after the weekly-bucket rename it still parsed the old "2006-01" monthly layout, so the over-time x-axis showed raw "2026-06-01" keys instead of "Jun 01". Caught while building --charts. Fixed in tokenops AND regenerated the already-live blog charts (they'd shipped with the buggy labels).
- **klarlabs pushes hit non-fast-forward repeatedly** (shared repo, image-bot commits land between) → stash pending post, fetch+rebase, push, stash pop. Standard dance.

### Open Questions
- (none) — both WAITING items from earlier still stand (optional live-verify a provider).

### Tasks
Done: timespan fix (verified against events store); published "The tool was guessing" post (live, deployed); --charts flag + chartDef registry (tested, lint-clean); periodLabel fix + regenerated live blog charts; v0.35.0 released (assets + brew 0.35.0). All CI-green.
Remaining: nothing pressing. Optional: live-verify a provider; make more use of the over-time charts.
