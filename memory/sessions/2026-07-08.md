---
date: 2026-07-08
---
## Session — catalog drift correction (#149) + adapter false-drift fixes (#150)
User: "go for it" → work through the drift the all-provider refresh surfaced and
fix the catalog. Discipline: cross-check EVERY row against the authoritative
vendor page before editing (the Opus lesson — never single-source).

**#149 — vendor-verified catalog corrections (pricing.yaml):**
- Mistral (mistral.ai/pricing/api, current -latest): large $2/$6→$0.50/$1.50 (Large 3),
  medium $0.40/$2→$1.50/$7.50 (Medium 3.5), small $0.20/$0.60→$0.15/$0.60 (Small 4).
  codestral UNCHANGED ($0.30/$0.90) — LiteLLM's codestral-latest ($1/$3) was the stale one.
- DeepSeek (api-docs.deepseek.com + LiteLLM's own deepseek-v4-flash row): deepseek-chat
  & deepseek-reasoner are deprecated aliases (retired 2026-07-24) for deepseek-v4-flash;
  both $0.27/$1.10 & $0.55/$2.19 → $0.14/$0.28 cache $0.0028.
- o1 gains cache-read $7.50; grok-3 gains cache-read $0.75 (base $3/$15 unchanged).
- NOT changed (verified FALSE drift): gpt-3.5-turbo ($0.50/$1.50), gemini-1.5-flash.

**#150 — two adapter false-drift bugs the refresh exposed (snapshot overrides baseline
at runtime, so these mis-price real usage):**
1. MMDD misorder: versionDate matched any 4-digit suffix; OpenAI MMDD (gpt-3.5-turbo-1106
   Nov > -0125 Jan numerically) picked the older pricier SKU. datedScore now gates 4-digit
   to plausible YYMM (year 20-29, month 01-12) — Mistral's YYMM still orders, OpenAI's MMDD
   drops to bare tier where the undated alias wins.
2. Distinct-SKU bleed: grok-3* swallowed grok-3-fast ($5/$25) & grok-3-mini. mapModelKey now
   only matches at a token boundary whose next token isn't a SKU qualifier (fast/mini/lite/
   pro/vision/…); those surface under their own keys.
   Verified live: gpt-3.5-turbo→$0.50/$1.50, grok-3→$3/$15/$0.75.

**Key finding:** after both PRs, ALL residual baseline-vs-current drift is LiteLLM-side
STALENESS, not catalog error — deepseek-chat alias ($0.28/$0.42 stale vs vendor v4-flash
$0.14/$0.28) and mistral-small ($0.06/$0.18 = LiteLLM still on Small 3.2 vs vendor Small 4
$0.15/$0.60). Our hand-verified baseline is now AHEAD of LiteLLM on these. gemini cache-read
rows remain unverified (left for a future pass).

## Decisions Made
- Cross-check every drifted row against the vendor page before editing pricing.yaml — never single-source (Opus lesson reinforced).
- Track Mistral catalog keys to the current "-latest" generation (Large 3 / Medium 3.5 / Small 4), not the historical SKU the key name suggests.
- Fix the adapter's collision heuristic edges (MMDD gating, SKU-qualifier boundary) rather than accept false drift, since the runtime snapshot overrides the baseline.

## What Went Wrong
- My initial hunch that mistral-medium +275% was a false positive was WRONG — the vendor confirmed medium-latest IS Medium 3.5 at $1.50/$7.50. Cross-checking (not assuming) caught it. Reinforces: verify, don't reason from priors.

## Open Questions
- Does the main cost-compute path use the effective engine (snapshots override baseline) or DefaultTable (baseline only)? If snapshots override, an adopted LiteLLM snapshot REGRESSES the vendor-verified deepseek/mistral-small baseline back to LiteLLM's stale values. May need a "pinned/verified baseline row" concept or a guard that won't adopt a snapshot row that contradicts a hand-verified baseline.

## Tasks
Done: #149 catalog corrections + #150 adapter fixes, both admin-merged (user pre-approved). Memory captured.
Remaining: verify the runtime pricing path (snapshot vs baseline precedence); optional release of #147-#150; gemini cache-read verified pass.
EOF2
echo "session written"
## Session 2 — snapshot-vs-baseline precedence fix (#151, verified-row pinning)
User: "First the snapshot vs baseline." Traced the runtime pricing path:
buildSpendEngine (bootstrap.go) returns the EFFECTIVE engine (flat baseline is
only a fallback). SnapshotsToDatedTables layers each snapshot as
DefaultTable().MergeOverrides(snapshot), effective-dated by FetchedAt → the
LATEST snapshot wins for current events. CONFIRMED live: despite #149 correcting
deepseek-chat to $0.14/$0.28, the adopted LiteLLM snapshot still priced it
$0.28/$0.42 (LiteLLM leaves the deprecated alias stale); same for mistral-small
(LiteLLM Small 3.2 $0.06/$0.18 vs vendor Small 4 $0.15/$0.60). The Opus lesson,
live: an automated source silently overriding human verification — and the
consistency guard is Anthropic-only, so no protection on these.

Also found: snapshots are dated by FetchedAt, NOT price-effective-from — so a
stale source rate gets stamped "effective <fetch date>", a small lie.

FIX (user chose "pin verified rows" over "baseline-authoritative" / "document"):
`verified: true` marker on a catalog row → spend.DefaultPinnedKeys() → 
SnapshotsToDatedTables strips pinned keys from each snapshot's override table
(base.MergeOverrides(snap.Table().Without(pinned))) so baseline wins at every
period. Unpinned rows still auto-adopt; new models still surface. Pinned the
non-Anthropic vendor-verified rows (mistral large/medium/small/codestral,
deepseek chat/reasoner, o1, grok-3). Verify-then-pin = the hardening workflow.
Test: TestEffectiveEngine_VerifiedRowResistsStaleSnapshot (stale deepseek snapshot
loses to pinned baseline; unpinned gpt-4o still adopts). Admin-merged (#151).

GOTCHA: pricing show/diff render the RAW snapshot (provenance), so pinned rows
DISPLAY the stale source value there and diff lists them as drift — even though
the cost engine uses the pinned baseline. Fast-follow: annotate those views with
pin status so diff doesn't tempt a human to "fix" the baseline toward the stale
value.

## Session 2 Decisions
- Pin verified rows (not baseline-authoritative, not document-only): keeps ADR 0002 effective-dating + auto-adopt for unpinned models, surgically protects hand-verified rows. Verify-then-pin is the workflow.
- Pin the non-Anthropic vendor-verified rows specifically — that's the exact gap the Anthropic-only ratio guard leaves open.

## Session 2 Open
- pricing show/diff pin-awareness (fast-follow, see gotcha above).

## Session 2 addendum — pin-awareness in diff/show (#152)
Closed the fast-follow: pricing diff/show render the raw snapshot, so pinned rows
(deepseek-chat/reasoner, mistral-*, o1, grok-3) now show a [pinned] marker + a
legend clarifying the cost engine uses the baseline — and diff warns not to
"correct" the baseline toward the shown drift. New PinnedSnapshotKeys() maps
catalog pin keys → snapshot key space. Presentational only; admin-merged.
Five-PR pricing arc complete: #147 (all-provider source) → #148 (collision) →
#149 (vendor catalog fix) → #150 (adapter false-drift) → #151 (verified pinning)
→ #152 (pin-aware diff/show). All merged to main, UNRELEASED.

## Session 2 addendum 2 — RELEASED v0.41.0
Cut v0.41.0 (#153 changelog → tag → goreleaser). GitHub release published (5 assets),
Homebrew tap bumped to 0.41.0, release workflow green (~2.5m). Covers the full arc:
#147 all-provider source, #148 collision, #149 vendor catalog corrections, #150 adapter
false-drift, #151 verified-row pinning, #152 pin-aware diff/show. NOTE: CHANGELOG-only PR
tripped the CI path-filter gap again (only nox ran; merged via --admin since code was
already green in the component PRs). The path-filter gap (include CHANGELOG/pricing.yaml)
is still open.

## Session 2 addendum 3 — CI path-filter fix (#154)
Fixed the CI gap + found a bigger latent bug. (1) ci.yml paths-ignore skipped CI on
doc/CHANGELOG-only PRs → required checks unreported → BLOCKED → --admin (the v0.41.0
release PR). Removed paths-ignore from the pull_request trigger only (kept on push for
cheap direct doc pushes to main). (2) BONUS BUG: branch protection requires
"Test (ubuntu-latest)" but the test job had dropped its OS matrix and reported bare
"Test" — the required context was permanently unsatisfiable, so EVERY PR needed --admin
(not just the classifier). Restored os:[ubuntu-latest] matrix. #154 hit mergeStateStatus
CLEAN and merged via a NORMAL squash (no --admin) — proof the whole thing works now.
Correction: pricing.yaml/testdata never skipped CI (they're not .md); the memory note was wrong.

## Session 2 addendum 4 — Actions-minute reduction (#155)
User flagged the account is over the 3k Actions-minute budget (and #154 cut the
wrong way — doc PRs now ran CI). Measured ci.yml per run: Test 95s(2min), Lint
30s(1), Bench 20s(1), Build ×5 ~47s each (5 min) → build matrix ≈55% of a run and
the least valuable per-PR (go test/vet already compile on linux; goreleaser cross-
compiles at release). Gated the build job to `github.event_name=='push'` — runs
post-merge on main (full matrix, coverage preserved; VERIFIED 5 build jobs on the
push run) but skipped on PRs (VERIFIED "Build: skipping", PR still CLEAN/mergeable,
required checks report → no --admin regression). Saves ~5 min per PR.
Further levers if needed: add a 1-job linux/amd64 canary build on PRs (~1 min) for
pre-merge cross-compile signal; security.yml (nox) runs on push+PR+weekly — could
trim. Kept #154's PR-runs-CI (doc PRs cost ~4 min, no --admin) rather than revert.

---
## END-OF-SESSION CAPTURE (consolidated, 2026-07-08)

Full day = one long arc: pricing accuracy → snapshot/baseline correctness → release → CI health/cost. 9 PRs merged (#147–#155), released **v0.41.0**.

### Decisions Made
- **Cross-check every drifted catalog row against the vendor page before editing** (never single-source) — reinforced by the Opus incident; caught my wrong mistral-medium hunch.
- **Mistral catalog keys track the current `-latest` generation** (Large 3 / Medium 3.5 / Small 4), not the SKU the key name implies.
- **Fix the adapter's collision heuristics** (newest-dated SKU wins; YYMM-vs-MMDD gating; SKU-qualifier boundary) rather than tolerate false drift — the runtime snapshot overrides baseline, so false drift mis-prices real usage.
- **Snapshot-vs-baseline: verified-row pinning** (not baseline-authoritative, not doc-only) — `verified: true` rows are stripped from snapshot overrides; keeps ADR 0002 effective-dating + auto-adopt for unpinned rows. Pinned the non-Anthropic vendor-verified rows (the gap the Anthropic-only guard leaves).
- **CI: PRs always run required checks** (dropped `paths-ignore` on the PR trigger; kept on push) so doc/release PRs report and merge without `--admin`.
- **CI: restore single-OS matrix so the test job reports `Test (ubuntu-latest)`** — matching the (stale) required context; this, not the classifier alone, was why every merge needed `--admin`.
- **CI: build matrix gated to `push` only** — ~5 min saved/PR; main + release keep full cross-compile. Chose this over reverting #154 (keeps no-admin doc PRs at ~4 min).

### What Went Wrong
- **My mistral-medium "false positive" hunch was wrong** — I assumed the +275% drift was an artifact; the vendor confirmed Medium 3.5 really is $1.50/$7.50. Cross-checking (not reasoning from priors) caught it. Lesson: verify, don't assume — again.
- **A memory note was inaccurate**: it claimed `pricing.yaml`/testdata changes skipped CI. They're `.yaml`/`.json`, never matched `**.md`, always got CI. The real gap was doc-only PRs. Corrected in the CI work.
- **#154 (CI path-filter fix) cut Actions minutes the wrong way** — making PRs always run CI (incl. the 5-way build) added minutes right when the account was over its 3k budget. Fixed by #155 (build push-only). Lesson: weigh Actions-minute cost when changing CI triggers.
- **Discovered a latent config bug**: branch protection required `Test (ubuntu-latest)` while the job reported `Test` — permanently unsatisfiable, forcing `--admin` on every merge for who-knows-how-long. Not caused this session, but surfaced + fixed.

### Open Questions
- Does `security.yml`/nox (runs push + PR + weekly) or `Test -race` (95s) warrant further Actions-minute trimming? Deferred — "good for now" per user.
- gemini-2.5 cache-read + gemini-1.5-flash($0) rows remain unverified (left unpinned/unchanged).

### Tasks
Done: pricing arc #147–#152; released v0.41.0 (#153); CI required-checks + stale-name fix (#154); Actions-minute reduction via push-only build (#155). All merged, main clean, memory captured.
Remaining (all optional/deferred): gemini-2.5 cache-read verified pass; deeper Actions-minute cuts (PR canary build, security.yml scoping, -race split); older backlog (ADR 0001 Phase 2+, OpenAI-compat live-verify, plan-catalog tiers/Bedrock).
